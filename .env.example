# ============================================================================
# PrivaChat Agents - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file with real credentials!

# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

# OpenRouter API Configuration
# Get your free API key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_MODEL=openrouter/auto
# Optional: Override base URL (defaults to https://openrouter.ai/api/v1)
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Database Configuration
# Use a strong, unique password for production!
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_USER=research_user
POSTGRES_DB=research_db
DATABASE_URL=postgresql+asyncpg://research_user:${POSTGRES_PASSWORD}@postgres:5432/research_db

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# Redis Cache
REDIS_URL=redis://redis:6379/0
REDIS_ENABLED=true

# SearxNG Search Engine
SEARXNG_API_URL=http://searxng:8080

# Server Configuration
API_HOST=0.0.0.0
API_PORT=8001
LOG_LEVEL=INFO

# ============================================================================
# LANGFUSE MONITORING (Optional)
# ============================================================================
# Uncomment and configure if you want to use Langfuse for LLM monitoring
# Get your keys at: https://cloud.langfuse.com

# LANGFUSE_ENABLED=false
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_SECRET_KEY=sk-lf-...
# LANGFUSE_HOST=https://cloud.langfuse.com

# ============================================================================
# ADVANCED SEARXNG CONFIGURATION (Optional)
# ============================================================================
# Fine-tune search behavior - all optional

# Comma-separated search engines (e.g., bing,google,duckduckgo)
# SEARXNG_ENGINES=google,bing,duckduckgo,qwant

# Comma-separated categories (e.g., general,science,news,IT)
SEARXNG_CATEGORIES=general,news,science,IT

# Number of result pages to fetch per query (1-5 recommended)
SEARXNG_PAGES=3

# Default time range for results (day, week, month, year)
# SEARXNG_TIME_RANGE=week

# Language code (ISO 639-1, e.g., en, es, fr, de)
# SEARXNG_LANGUAGE=en

# Enable vendor site backfill (AWS, Azure, GCP documentation)
# SEARXNG_BACKFILL_ENABLED=true

# ============================================================================
# LLM MODEL CONFIGURATION (Optional)
# ============================================================================
# Override default models for specific tasks

# Model for hallucination correction/regeneration (default: google/gemini-2.5-flash-lite)
REGENERATION_LLM_MODEL=google/gemini-2.5-flash-lite

# Research/primary LLM model (default: google/gemini-2.5-flash-lite)
# RESEARCH_LLM_MODEL=google/gemini-2.5-flash-lite

# Fallback LLM model (default: google/gemini-2.5-flash-lite)
# FALLBACK_LLM_MODEL=google/gemini-2.5-flash-lite

# ============================================================================
# DOCKER COMPOSE PORT MAPPING
# ============================================================================
# These are the HOST ports (what you access on your machine)
# The container ports are fixed in docker-compose.yml

# API Server: http://localhost:8001
# Streamlit UI: http://localhost:8503
# PostgreSQL: localhost:5433
# Redis: localhost:6380
# SearxNG: http://localhost:4000



